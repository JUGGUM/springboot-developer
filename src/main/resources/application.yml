spring:
  jpa:
          # 전송 쿼리 확인
          show-sql: true
          properties:
            hibernate:
              format_sql: true
          # 테이블 생성 후에 data.sql 에있는 쿼리를 실행하도록 하는 옵션
          defer-datasource-initialization: true
